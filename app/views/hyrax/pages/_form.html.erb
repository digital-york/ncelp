<div class="panel panel-default tabs">
  <ul class="nav nav-tabs" role="tablist">
    <li class="active">
      <a href="#about" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.about_page') %></a>
    </li>
    <li>
      <a href="#help" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.help_page') %></a>
    </li>
    <li>
      <a href="#agreement" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.agreement_page') %></a>
    </li>
    <li>
      <a href="#terms" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.terms_page') %></a>
    </li>
    <li>
      <a href="#schemes_of_work" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.schemes_of_work_page') %></a>
    </li>
    <li>
      <a href="#all_collections" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.all_collections_page') %></a>
    </li>    
    <li>
      <a href="#cite" role="tab" data-toggle="tab"><%= t(:'hyrax.pages.tabs.cite_page') %></a>
    </li>
  </ul>
  <div class="tab-content">
    <div id="about" class="tab-pane active">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:about), url: hyrax.page_path(ContentBlock.for(:about)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :about %><br />
                <%= f.text_area :about, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="help" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:help), url: hyrax.page_path(ContentBlock.for(:help)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :help %><br />
                <%= f.text_area :help, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="agreement" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:agreement), url: hyrax.page_path(ContentBlock.for(:agreement)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :agreement %><br />
                <%= f.text_area :agreement, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="terms" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:terms), url: hyrax.page_path(ContentBlock.for(:terms)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :terms %><br />
                <%= f.text_area :terms, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="schemes_of_work" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:schemes_of_work), url: hyrax.page_path(ContentBlock.for(:schemes_of_work)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :schemes_of_work %><br />
                <%= f.text_area :schemes_of_work, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="all_collections" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:all_collections), url: hyrax.page_path(ContentBlock.for(:all_collections)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :all_collections %><br />
                <%= f.text_area :all_collections, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
    <div id="cite" class="tab-pane">
      <div class="panel panel-default labels">
        <%= simple_form_for ContentBlock.for(:cite), url: hyrax.page_path(ContentBlock.for(:cite)) do |f| %>
          <div class="panel-body">
            <div class="field form-group">
                <%= f.label :cite %><br />
                <%= f.text_area :cite, value: f.object.value, class: 'form-control tinymce', rows: 20, cols: 120 %>
            </div>
          </div>
          <div class="panel-footer">
            <%= link_to t(:'hyrax.pages.cancel'), hyrax.admin_admin_sets_path, class: 'btn btn-default pull-right' %>
            <%= f.button :submit, class: 'btn btn-primary pull-right' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script>
    try{
        tinymce.remove();
        tinyMCE.init({
            selector: 'textarea.tinymce',
            height: 500,
            plugins: 'media link image lists table',
            menubar: false,
            toolbar: 'styleselect | formats | undo redo | ' +
                'bold italic underline strikethrough | ' +
                'numlist bullist | alignleft aligncenter alignright alignjustify  | ' +
                'image media link | table tabledelete | ' +
                'tableprops tablerowprops tablecellprops | ' +
                'tableinsertrowbefore tableinsertrowafter tabledeleterow | ' +
                'tableinsertcolbefore tableinsertcolafter tabledeletecol'
        })
    }catch(error){}
</script>