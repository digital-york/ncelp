<%= f.input :language, as: :check_boxes,
            collection: GenericLocalAuthorityService.select_all_options('language'),
            value_method: lambda {|v| v.last.to_s },
            input_html: { multiple: true  }
%>